---
const currentPath = Astro.url.pathname;
const isEnglish = currentPath.startsWith('/en');

const koNavItems = [
  { path: '/loan', label: '대출 계산기', category: '금융' },
  { path: '/salary', label: '연봉 계산기', category: '금융' },
  { path: '/tax-refund', label: '연말정산', category: '금융' },
  { path: '/compound', label: '복리 계산기', category: '금융' },
  { path: '/severance', label: '퇴직금', category: '금융' },
  { path: '/bmi', label: 'BMI', category: '건강' },
  { path: '/calorie', label: '칼로리', category: '건강' },
  { path: '/tdee', label: '기초대사량', category: '건강' },
  { path: '/due-date', label: '출산예정일', category: '생활' },
  { path: '/dday', label: 'D-day', category: '생활' },
];

const enNavItems = [
  { path: '/en/mortgage-calculator', label: 'Mortgage', category: 'Finance' },
  { path: '/en/salary-calculator', label: 'Salary', category: 'Finance' },
  { path: '/en/compound-interest-calculator', label: 'Compound', category: 'Finance' },
  { path: '/en/percentage-calculator', label: 'Percentage', category: 'Math & Utility' },
  { path: '/en/tip-calculator', label: 'Tip', category: 'Math & Utility' },
  { path: '/en/age-calculator', label: 'Age', category: 'Math & Utility' },
  { path: '/en/bmi-calculator', label: 'BMI', category: 'Health' },
  { path: '/en/tdee-calculator', label: 'TDEE', category: 'Health' },
  { path: '/en/due-date-calculator', label: 'Due Date', category: 'Health' },
  { path: '/en/unit-converter', label: 'Unit Converter', category: 'Math & Utility' },
];

const navItems = isEnglish ? enNavItems : koNavItems;
const homeHref = isEnglish ? '/en/' : '/';
const langToggleHref = isEnglish ? '/' : '/en/';
const categories = isEnglish ? ['Finance', 'Health', 'Math & Utility'] : ['금융', '건강', '생활'];
---

<header class="bg-white border-b border-gray-200 sticky top-0 z-50 shadow-sm">
  <div class="max-w-4xl mx-auto px-4">
    <div class="flex items-center justify-between h-16">
      <!-- 로고 -->
      <a href={homeHref} class="flex items-center gap-2 font-bold text-xl text-primary-700 hover:text-primary-600 transition-colors" aria-label="mycalcstool home">
        <svg width="28" height="28" viewBox="0 0 28 28" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
          <rect width="28" height="28" rx="6" fill="#2563EB"/>
          <rect x="6" y="6" width="7" height="7" rx="1.5" fill="white"/>
          <rect x="15" y="6" width="7" height="7" rx="1.5" fill="white"/>
          <rect x="6" y="15" width="7" height="7" rx="1.5" fill="white"/>
          <rect x="15" y="15" width="7" height="7" rx="1.5" fill="#93C5FD"/>
        </svg>
        <span>mycalcstool</span>
      </a>

      <!-- 데스크톱 네비게이션 -->
      <nav class="hidden md:flex items-center gap-1" aria-label="주요 계산기">
        {navItems.slice(0, 6).map(item => (
          <a
            href={item.path}
            class={`px-3 py-2 rounded-lg text-sm font-medium transition-colors ${
              currentPath === item.path
                ? 'bg-primary-50 text-primary-700'
                : 'text-gray-600 hover:text-primary-600 hover:bg-gray-50'
            }`}
            aria-current={currentPath === item.path ? 'page' : undefined}
          >
            {item.label}
          </a>
        ))}
        <a href={homeHref} class="px-3 py-2 text-sm text-gray-500 hover:text-primary-600 transition-colors">
          {isEnglish ? 'More' : '더보기'}
        </a>
        <!-- 언어 전환 버튼 -->
        <a
          href={langToggleHref}
          class="ml-2 px-3 py-1.5 rounded-lg text-sm font-semibold border transition-colors border-primary-200 text-primary-700 hover:bg-primary-50"
          aria-label={isEnglish ? 'Switch to Korean' : 'Switch to English'}
        >
          {isEnglish ? '🇰🇷 KR' : '🇺🇸 EN'}
        </a>
      </nav>

      <!-- 모바일 햄버거 버튼 -->
      <button
        id="mobile-menu-btn"
        class="md:hidden p-2 rounded-lg text-gray-600 hover:bg-gray-100 transition-colors"
        aria-label="메뉴 열기"
        aria-expanded="false"
        aria-controls="mobile-menu"
        type="button"
      >
        <svg id="menu-open-icon" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <line x1="3" y1="6" x2="21" y2="6"/>
          <line x1="3" y1="12" x2="21" y2="12"/>
          <line x1="3" y1="18" x2="21" y2="18"/>
        </svg>
        <svg id="menu-close-icon" class="hidden" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <line x1="18" y1="6" x2="6" y2="18"/>
          <line x1="6" y1="6" x2="18" y2="18"/>
        </svg>
      </button>
    </div>
  </div>

  <!-- 모바일 메뉴 -->
  <nav id="mobile-menu" class="hidden md:hidden border-t border-gray-100 bg-white" aria-label="모바일 네비게이션">
    <div class="max-w-4xl mx-auto px-4 py-4">
      <!-- 모바일 언어 전환 -->
      <div class="flex justify-end mb-3">
        <a
          href={langToggleHref}
          class="px-3 py-1.5 rounded-lg text-sm font-semibold border border-primary-200 text-primary-700 hover:bg-primary-50 transition-colors"
        >
          {isEnglish ? '🇰🇷 한국어' : '🇺🇸 English'}
        </a>
      </div>
      <div class="grid grid-cols-2 gap-2">
        {categories.map(category => (
          <div class="col-span-2">
            <p class="text-xs font-semibold text-gray-400 uppercase tracking-wider mb-2">{category}</p>
            <div class="grid grid-cols-2 gap-2">
              {navItems.filter(item => item.category === category).map(item => (
                <a
                  href={item.path}
                  class={`px-3 py-2.5 rounded-lg text-sm font-medium transition-colors ${
                    currentPath === item.path
                      ? 'bg-primary-50 text-primary-700'
                      : 'text-gray-700 hover:bg-gray-50 hover:text-primary-600'
                  }`}
                >
                  {item.label}
                </a>
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  </nav>
</header>

<script>
  // 모바일 메뉴 토글 (XSS 안전: DOM 조작만)
  const btn = document.getElementById('mobile-menu-btn');
  const menu = document.getElementById('mobile-menu');
  const openIcon = document.getElementById('menu-open-icon');
  const closeIcon = document.getElementById('menu-close-icon');

  if (btn && menu && openIcon && closeIcon) {
    btn.addEventListener('click', () => {
      const isOpen = !menu.classList.contains('hidden');
      menu.classList.toggle('hidden');
      openIcon.classList.toggle('hidden');
      closeIcon.classList.toggle('hidden');
      btn.setAttribute('aria-expanded', String(!isOpen));
    });
  }
</script>
